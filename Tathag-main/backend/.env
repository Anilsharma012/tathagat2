PORT=5000
NODE_ENV=development

# Mongo / JWT (tumhare existing)
MONGO_URI=mongodb+srv://sachintakoria2204:Sachin%40123@cluster1.zutj6.mongodb.net/iquanta_db?retryWrites=true&w=majority
JWT_SECRET=Sachin@123

# --- SMTP (Gmail + App Password) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ethan2233har@gmail.com
SMTP_PASS=kgmkkpjvsozdcbhn
MAIL_FROM="TathaGat <ethan2233har@gmail.com>"

# --- Aliases (agar code EMAIL_/MAIL_ padhta ho) ---
EMAIL_HOST=${SMTP_HOST}
EMAIL_PORT=${SMTP_PORT}
EMAIL_USER=${SMTP_USER}
EMAIL_PASS=${SMTP_PASS}
EMAIL_PASSWORD=${SMTP_PASS}
MAIL_HOST=${SMTP_HOST}
MAIL_PORT=${SMTP_PORT}
MAIL_USER=${SMTP_USER}
MAIL_PASS=${SMTP_PASS}

# REACT_* frontend ke liye hote hain, backend ko zaroorat nahi.
# REACT_APP_BACKEND_URL=http://localhost:5000/api/v1
